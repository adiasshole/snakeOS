[Output]
Format=disk
OutputDirectory=%D/../build/images

[Content]
# Prefer the systemd "standard" rootfs for mainstream compatibility; adjust if you still use the Python-PID1 profile.
BaseTrees=%D/../build/rootfs-standard
