# Install as /etc/greetd/config.toml on the rootfs (see README desktop section).
# Create the login user first (e.g. `adduser snakeos`) and install sway, foot, etc.

[terminal]
vt = 7

[default_session]
command = "dbus-run-session sway"
user = "snakeos"
